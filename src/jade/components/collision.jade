include ../mixins/select_control
include ../mixins/coupled_form_groups

label Collision Type
div
    +select(this.id('collisionPicker'))
br

label Body Type
div
    +select(this.id('bodyPicker'))
br

label Collision Dimensions
+width_height_control("Collision Width", "Collision Height", "data.info.dimension.x", "data.info.dimension.y")

br

label One Way Normal
+x_y_control("One Way X", "One Way Y", "data.oneWayNormal.x", "data.oneWayNormal.y")

div.canvas-menu-btns
    button(rv-command-undo) Undo
    button(rv-command-redo) Redo
    button(rv-command-save) Save
    select(id=this.id("toolSelect"))
        option(value="select") Select Entity
        option(value="create") Create Entity
        option(value="move") Move Entity
div.canvas-div(id=this.id("canvas-div"))
    canvas(id=this.id("entity-canvas"),width="800",height="500")

import {DisplayObject} from 'pixi.js';

import {Vector} from '../math/vector';

function drawSelector(position : Vector) : DisplayObject {
    return null;
}

function indexOfSelection(mouseLocation : Vector, seletors : Vector []) {
    return 0; // TODO Implement
}
